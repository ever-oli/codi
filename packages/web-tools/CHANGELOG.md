## [Unreleased]

### Added

- Initial `web_search` and `web_extract` extension package.

### Changed

- Renamed tools to `quick_web_search` and `quick_web_extract` to avoid conflicts with `pi-web-access`.
