## [Unreleased]

### Added

- Initial `memory` extension package with persistent file-backed stores for `memory` and `user`, bounded char limits, frozen system-prompt snapshots, and mutation actions (`read`, `add`, `replace`, `remove`).
