# @mariozechner/pi-clarify-tools

Pi package that adds a `clarify` tool for structured clarification requests using Pi's interactive UI.

## Install

```bash
pi install /absolute/path/to/packages/clarify-tools
```

## Smoke Test

```text
Build a tiny feature, but if the request is ambiguous use clarify instead of guessing.
```
